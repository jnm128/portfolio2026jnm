

## Make "Beyond the Pixels" subtitles more prominent

### Change
In `src/pages/AboutPage.tsx`, update the subtitle labels (Writing, Speaking, Current Read, Work Playlist) from `text-white/60` to `text-white` so they stand out more against the dark background.

### File: `src/pages/AboutPage.tsx`
Four `<h2>` elements with class `text-white/60` → change to `text-white`:
1. "Writing" subtitle
2. "Speaking" subtitle
3. "Current Read" subtitle
4. "Work Playlist" subtitle

