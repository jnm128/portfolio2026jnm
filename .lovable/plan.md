

## Change corner radius to `rounded-2xl` across screens

**File:** `src/pages/BookClub.tsx`

Change the hero image container class from `rounded-3xl` to `rounded-2xl` (line ~115).

This is the only instance of `rounded-3xl` on the Book Club page that needs updating — the book carousel cards already use `rounded-xl`.

